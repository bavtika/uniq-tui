[package]
name = "jerasure-sys"
description = "Stub crate (sources not built by default vanity target)"
version = "0.1.0"
authors = ["uniq-tui"]
license = "MIT"
build = "build.rs"

[build-dependencies]
cc = "1.0"
